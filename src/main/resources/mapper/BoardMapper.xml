<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.board8.model.BoardMapper">

   <insert id="insBoard" parameterType="BoardDto">
       insert into t_board
       (title,ctnt,writer)
       values
       (#{title},#{ctnt},#{writer})
   </insert>
    <select id="selBoardAll" resultType="BoardVo">
    SELECT iboard,title,writer,
        created_at createdAt
        FROM t_board
    </select>

    <select id="selBoardById" resultType="BoardDetailVo">
        SELECT iboard,title,ctnt,writer,
        created_at createdAt,
        updated_at updatedAt
        FROM t_board
        where iboard = ${iboard}
    </select>

    <update id="upBoard">
        update t_board
        set
        title = #{title},
        ctnt = #{ctnt},
        writer = #{writer},
        updated_at = current_timestamp()
        where iboard = ${iboard}
    </update>

    <delete id="delBoard">
        delete from t_board
        where iboard = #{iboard}
    </delete>



</mapper>


